# Descifra .env.enc en memoria al entrar en el directorio.
# No deja el .env en claro en el árbol de trabajo.
if [ -f .env.enc ]; then
  _tmp="$(mktemp)"
  sops -d --input-type dotenv --output-type dotenv .env.enc > "$_tmp"
  dotenv "$_tmp"
  rm -f "$_tmp"
  unset _tmp
fi
