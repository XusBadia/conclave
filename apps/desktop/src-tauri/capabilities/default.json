{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Conclave desktop. Without this file Tauri 2 grants no permissions to the WebView — including core:window:allow-start-dragging, which data-tauri-drag-region depends on for window drag.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "fs:default",
    "os:default",
    "shell:default"
  ]
}
